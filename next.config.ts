// import withSerwistInit from '@serwist/next'

// const withSerwist = withSerwistInit({
//   swSrc: 'src/app/sw.ts',
//   swDest: 'public/sw.js',
// })

const nextConfig = {
  experimental: {
    viewTransition: true,
  },
}

export default nextConfig
